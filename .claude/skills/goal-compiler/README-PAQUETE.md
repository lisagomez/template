# goal-compiler — skill para tu agente

Paquete preparado para la comunidad SaaS Factory. Copia esta carpeta dentro de
`.claude/skills/` de tu proyecto y tu agente la detecta sola.

```bash
unzip goal-compiler.zip -d .claude/skills/
```

Contiene 4 archivos.
