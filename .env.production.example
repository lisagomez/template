# =============================================================================
# Produccion — Hetzner cx33. Copiar a .env.production EN EL SERVIDOR.
# .env.production esta en .gitignore: nunca se commitea.
# =============================================================================

# --- Dominio y TLS (Caddy) ---
DOMAIN=tuapp.com
TLS_EMAIL=tu@email.com

# --- Supabase: publicas (se INLINEAN en build) ---
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGci...
NEXT_PUBLIC_SITE_URL=https://tuapp.com

# --- Secretos server-side (se leen en RUNTIME, nunca llegan al navegador) ---
SUPABASE_SERVICE_ROLE_KEY=
OPENROUTER_API_KEY=
